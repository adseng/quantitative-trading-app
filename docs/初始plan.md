市场情绪受什么影响，我想做一个量化工具，分析各种可能影响情绪的因子，
归纳为两个情绪，看涨，看跌。
比如某某趋势是上涨，则看涨+1分，具体也可能不是加一分，要看这个影响的权重，当然也是可以调节，在大量的试验中反复调节。
当前假如是15分钟级别k线（这个也可以调节），当前k线收盘时，当看涨分数>看跌分数，则预测下一轮k线看涨。
帮我收集所有可能的因子，并写出软件。
软件使用wails框架，涉及go，ts，vite。
使用https://github.com/binance/binance-connector-go的sdk，websocket。
使用测试网的数据。
使用合约。
获取合约15分钟级别的数据，回放数据，做理论验证，自动调节 影响因子的权重也就是分数。


提示词：
不是的，你的测试脚本应该只调用GenerateTestCases方法，不断优化GenerateTestCases方法里面正在调用的单因子策略，我说测试20轮，意思是你需要执行这个测试脚本20次，每次执行完了分析结果，记录结果，优化代码。

采用手动迭代流程：

运行测试脚本 → 仅调用 GenerateTestCases() 并回测
分析结果并记录
根据最优参数更新 case_ma.go
重复 20 次